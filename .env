# Port exposed on the host for the Flask API
API_PORT=8080

# Absolute path on the host where downloaded videos are stored.
# This must match the media path configured in Home Assistant.
DOWNLOAD_DIR=/media/youtube_downloads

# Optional extra arguments passed directly to yt-dlp (space-separated).
# Quoted arguments are supported (parsed with shlex).
# Example: --cookies-from-browser chrome --format bestvideo+bestaudio
YT_DLP_EXTRA_ARGS=

# Maximum time in seconds allowed for a single download (default: 3600 = 1 hour).
DOWNLOAD_TIMEOUT=3600
